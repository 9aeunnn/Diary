import SwiftUI


struct 숫1버튼: View {
    var body: some View {
//        Button(action:{
//        }) {
            Image("1")
//        }
    }
}

struct 숫2버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("2")
        }
    }
}

struct 숫3버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("3")
        }
    }
}

struct 숫4버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("4")
        }
    }
}

struct 숫5버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("5")
        }
    }
}

struct 숫6버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("6")
        }
    }
}

struct 숫7버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("7")
        }
    }
}

struct 숫8버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("8")
        }
    }
}

struct 숫9버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("9")
        }
    }
}

struct 숫10버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("10")
        }
    }
}

struct 숫11버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("11")
        }
    }
}

struct 숫12버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("12")
        }
    }
}

struct 숫13버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("13")
        }
    }
}

struct 숫14버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("14")
        }
    }
}

struct 숫15버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("15")
        }
    }
}


struct 숫16버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("16")
        }
    }
}


struct 숫17버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("17")
        }
    }
}


struct 숫18버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("18")
        }
    }
}


struct 숫19버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("19")
        }
    }
}


struct 숫20버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("20")
        }
    }
}


struct 숫21버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("21")
        }
    }
}

struct 숫22버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("22")
        }
    }
}

struct 숫23버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("23")
        }
    }
}

struct 숫24버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("24")
        }
    }
}

struct 숫25버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("25")
        }
    }
}


struct 숫26버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("26")
        }
    }
}

struct 숫27버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("27")
        }
    }
}

struct 숫28버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("28")
        }
    }
}

struct 숫29버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("29")
        }
    }
}

struct 숫30버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("30")
        }
    }
}

struct 숫31버튼: View {
    var body: some View {
        Button(action:{
        }) {
            Image("31")
        }
    }
}

struct 사진넣기: View {
    var body: some View {
        Button(action:{
        }) {
            Image("사진넣기")
        }
    }
}



